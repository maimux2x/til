## zmv コマンド

複数ファイルの一括リネームをしたいときに便利に使える

```
$ zmv 'パターン' '置換パターン'
```

`.txt`の拡張子を`.md`に一括置換したい場合などに


```
$ zmv '*.txt' '*.md'
```

のように使うことができる

`-n`オプションをつけると実行せずに変更内容を確認することが可能

```
$ zmv -nW '*.txt' '*.md'
mv -- a.txt a.md
mv -- b.txt b.md
```
